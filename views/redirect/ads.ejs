<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= siteName %> - Redirecting</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free/css/all.min.css" rel="stylesheet">
  <style>
    /* ===== Global Layout ===== */
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(180deg, #f8f9fa, #e9ecef);
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center; /* Pasti center horizontal + vertical */
      padding: 1rem;
      overflow-x: hidden;
    }

    /* ===== Card ===== */
    .redirect-card {
      max-width: 420px;
      width: 100%;
      background: #fff;
      border-radius: 18px;
      padding: 2rem;
      text-align: center;
      box-shadow: 0 12px 32px rgba(0,0,0,.08);
      animation: fadeInUp .6s ease both;
      position: relative;
      overflow: hidden;
    }
    .redirect-card::before {
      content: "";
      position: absolute;
      top: -50%; left: -50%;
      width: 200%; height: 200%;
      background: radial-gradient(circle at center, rgba(13,110,253,.08), transparent 70%);
      z-index: 0;
    }
    .redirect-card > * { position: relative; z-index: 1; }

    /* ===== Icon ===== */
    .redirect-icon {
      font-size: 52px;
      color: #0d6efd;
      margin-bottom: 1rem;
      animation: pulse 1.5s infinite;
    }

    /* ===== Text ===== */
    .redirect-card h3 {
      font-weight: 600;
      font-size: 1.3rem;
      margin-bottom: .4rem;
    }
    .redirect-card p {
      font-size: .95rem;
      color: #6c757d;
      margin-bottom: 1.5rem;
    }

    /* ===== Progress ===== */
    .progress {
      height: 12px;
      border-radius: 20px;
      overflow: hidden;
      margin-top: 1.2rem;
      background: #e9ecef;
      box-shadow: inset 0 2px 6px rgba(0,0,0,.08);
    }
    .progress-bar {
      width: 0%;
      height: 100%;
      background: linear-gradient(90deg, #0d6efd, #0b5ed7);
      transition: width 1s linear;
    }

    /* ===== Button ===== */
    #continueBtn {
      margin-top: 1.5rem;
      min-width: 200px;
      font-size: 1rem;
      font-weight: 600;
      border-radius: 10px;
      padding: .7rem 1.2rem;
      transition: all .3s ease;
    }
    #continueBtn:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }
    #continueBtn:hover:not(:disabled) {
      transform: translateY(-2px) scale(1.02);
      box-shadow: 0 8px 20px rgba(13,110,253,.3);
    }

    /* ===== Animations ===== */
    @keyframes fadeInUp {
      0% { transform: translateY(20px); opacity: 0; }
      100% { transform: translateY(0); opacity: 1; }
    }
    @keyframes pulse {
      0%,100% { transform: scale(1); opacity: 1; }
      50% { transform: scale(1.1); opacity: .9; }
    }

    /* ===== Responsive ===== */
    @media (max-width: 480px) {
      .redirect-card { padding: 1.2rem; border-radius: 14px; }
      .redirect-icon { font-size: 40px; }
      .redirect-card h3 { font-size: 1.1rem; }
      .redirect-card p { font-size: .85rem; }
      #continueBtn { font-size: .9rem; padding: .6rem; }
    }

    /* ===== Hidden Iframes ===== */
    iframe.adframe { display:none; width:0; height:0; border:0; }
    .adsbox { height: 1px; width: 100%; }
  </style>
</head>
<body>
  <!-- Redirect Card -->
  <div class="redirect-card">
    <div class="redirect-icon"><i class="fa fa-spinner fa-spin"></i></div>
    <h3>Sedang mengarahkan...</h3>
    <p>Mohon tunggu sebentar, lalu klik tombol untuk melanjutkan.</p>
    <div class="progress"><div id="progressBar" class="progress-bar"></div></div>
    <button id="continueBtn" class="btn btn-primary mt-3" disabled>Lanjutkan</button>
  </div>

  <!-- Anti-adblock bait -->
  <div class="adsbox"></div>

  <!-- Iframe pertama: auto load saat halaman dibuka -->
  <iframe class="adframe" src="<%= adUrl %>"></iframe>

  <!-- Iframe kedua: load setelah tombol diklik -->
  <iframe id="adframe2" class="adframe"></iframe>

  <!-- Adsterra script kalau ada -->
  <%- adScript %>

  <script>
    const bar = document.getElementById('progressBar');
    const btn = document.getElementById('continueBtn');
    const adUrl = "<%= adUrl %>";
    const targetUrl = "<%= target %>";
    const totalSteps = 10; // progress 10 detik
    let current = 0;

    const timer = setInterval(() => {
      current++;
      bar.style.width = (current * 10) + "%";

      if (current >= totalSteps) {
        clearInterval(timer);
        btn.disabled = false;
        btn.innerHTML = "Lanjutkan <i class='fa fa-arrow-right'></i>";
      }
    }, 1000);

    btn.addEventListener('click', () => {
      // Load iframe kedua (counted klik #2)
      document.getElementById('adframe2').src = adUrl;

      // Setelah delay, redirect user ke target
      setTimeout(() => {
        window.location.href = targetUrl;
      }, 2000);
    });
  </script>
</body>
</html>
