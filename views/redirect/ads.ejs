<!DOCTYPE html>
<html lang="id">
<head>
  <% include ./partials/head %>
  <title>Mengalihkan...</title>
  <style>
    body {
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
      background: radial-gradient(1000px 600px at center top, var(--primary-soft), transparent 80%);
      transition: background var(--transition);
      padding: 1rem;
    }
    .redirect-card {
      width: 100%;
      max-width: 420px;
      background: var(--card);
      color: var(--text);
      border-radius: var(--radius);
      border: 1px solid var(--border);
      box-shadow: var(--shadow);
      padding: 1.8rem;
      text-align: center;
      animation: fadeInUp .6s ease both;
    }
    .redirect-icon {
      font-size: 48px;
      color: var(--primary);
      margin-bottom: 1rem;
      animation: pulse 1.8s infinite;
    }
    .redirect-card h3 {
      font-weight: 600;
      font-size: 1.25rem;
      margin-bottom: .4rem;
    }
    .redirect-card p {
      font-size: .95rem;
      color: var(--muted);
      margin-bottom: 1.5rem;
    }
    .progress {
      height: 8px;
      border-radius: 20px;
      overflow: hidden;
      margin-bottom: 1.2rem;
      background: var(--border);
    }
    .progress-bar {
      width: 0%;
      height: 100%;
      background: linear-gradient(90deg, var(--primary), var(--primary-600));
      transition: width 1s linear;
    }
    .redirect-btn {
      width: 100%;
      font-size: 1rem;
      padding: .7rem;
      border-radius: var(--radius-sm);
      font-weight: 600;
      letter-spacing: .3px;
      transition: all var(--transition);
    }
    .redirect-btn:disabled {
      background: var(--muted);
      border: none;
      color: #fff;
      cursor: not-allowed;
    }
    .adsbox { height: 1px; width: 100%; }
    @keyframes fadeInUp {
      0% { transform: translateY(15px); opacity: 0; }
      100% { transform: translateY(0); opacity: 1; }
    }
    @keyframes pulse {
      0%,100% { transform: scale(1); opacity: 1; }
      50% { transform: scale(1.1); opacity: .9; }
    }
    /* Responsif HP */
    @media (max-width: 480px) {
      .redirect-card { padding: 1.2rem; border-radius: var(--radius-sm); }
      .redirect-icon { font-size: 36px; }
      .redirect-card h3 { font-size: 1.1rem; }
      .redirect-card p { font-size: .85rem; }
      .redirect-btn { font-size: .9rem; padding: .6rem; }
    }
  </style>
</head>
<body>
  <div class="redirect-card">
    <div class="redirect-icon"><i class="fa fa-shield-alt"></i></div>
    <h3>Mengalihkan dengan Aman...</h3>
    <p>Kami sedang memverifikasi tautan anda, mohon tunggu sebentar.</p>

    <div class="progress">
      <div class="progress-bar" id="progressBar"></div>
    </div>

    <button id="continueBtn" class="btn btn-primary redirect-btn" disabled>
      Menunggu...
    </button>

    <!-- bait adblock -->
    <div class="adsbox"></div>
  </div>

  <script>
    let progress = 0;
    const bar = document.getElementById("progressBar");
    const btn = document.getElementById("continueBtn");
    const targetUrl = "<%= url %>";

    // total durasi ±10 detik → naik 10% tiap 1 detik
    const timer = setInterval(() => {
      progress += 10;
      bar.style.width = progress + "%";
      if (progress >= 100) {
        clearInterval(timer);
        btn.disabled = false;
        btn.innerText = "Klik Untuk Melanjutkan";
      }
    }, 1000);

    btn.addEventListener("click", () => {
      // trigger iklan terselubung / event tracking
      setTimeout(() => { window.location.href = targetUrl; }, 400);
    });

    // deteksi adblock
    window.addEventListener("load", () => {
      const bait = document.querySelector(".adsbox");
      if (!bait || bait.offsetHeight === 0) {
        toastr.warning("Nonaktifkan Adblock agar tautan berfungsi!");
      }
    });
  </script>
</body>
</html>
